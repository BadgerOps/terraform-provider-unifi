---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "unifi_acl_rule_ordering Resource - unifi"
subcategory: ""
description: |-
  Manage the ordered ACL rule list for a UniFi site.
---

# unifi_acl_rule_ordering (Resource)

Manage the ordered ACL rule list for a UniFi site.

## Example Usage

```terraform
# Control the ordering of user-defined ACL rules on a site.
data "unifi_site" "main" {
  name = "Default"
}

resource "unifi_network" "iot" {
  site_id                 = data.unifi_site.main.id
  management              = "GATEWAY"
  name                    = "iot"
  enabled                 = true
  vlan_id                 = 30
  isolation_enabled       = true
  cellular_backup_enabled = false
  internet_access_enabled = true
  mdns_forwarding_enabled = false

  ipv4_configuration = {
    auto_scale_enabled = false
    host_ip_address    = "10.30.0.1"
    prefix_length      = 24
  }
}

resource "unifi_acl_rule" "block_iot_dns" {
  site_id         = data.unifi_site.main.id
  type            = "IPV4"
  enabled         = true
  name            = "block-iot-dns"
  action          = "BLOCK"
  protocol_filter = ["TCP", "UDP"]

  source_ip_filter = {
    type        = "NETWORKS"
    network_ids = [unifi_network.iot.id]
  }

  destination_ip_filter = {
    type  = "PORTS"
    ports = [53]
  }
}

resource "unifi_acl_rule_ordering" "site" {
  site_id              = data.unifi_site.main.id
  ordered_acl_rule_ids = [unifi_acl_rule.block_iot_dns.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `ordered_acl_rule_ids` (List of String) Ordered ACL rule UUIDs from highest priority to lowest priority.
- `site_id` (String) Site UUID whose ACL rule ordering is managed.

### Read-Only

- `id` (String) Synthetic resource ID equal to `site_id`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import ACL rule ordering by site identifier.
terraform import unifi_acl_rule_ordering.site <site_id>
```
